# Initialize an empty list to store the results
result = []

# Iterate through numbers from 1500 to 2700 (both inclusive)
for num in range(1500, 2701):
    # Check if the number is divisible by 7 and a multiple of 5
    if num % 7 == 0 and num % 5 == 0:
        # If true, append the number to the result list
        result.append(num)

# Print the numbers that satisfy the condition
print("Numbers between 1500 and 2700 divisible by 7 and multiple of 5:")
print(result)
